BALI VS STARGATE - SHARED FOLDER BRIDGE V012E

Safe bridge idea:
1. Bali CPU writes SIM-only check-in JSON to from_bali.
2. Stargate CPU writes SIM-only check-in JSON to from_stargate.
3. Referee reads both JSON files and scores the round.
4. No executable files. No API keys. No live orders. No remote commands.

You can use this folder locally first, then later point both PCs at a Windows shared folder.
Keep bridge mode as JSON-only until the referee layer has enough paper/sim evidence.
