BALI V041 AUTOMATIC FOREVER STARTER

Double-click:
BALI START HERE - ONE CLICK.bat

What it now does:
- Self-locates from the folder it is in instead of assuming Desktop.
- Applies one valid autopatch ZIP if present.
- Finds a usable Python 3.10+ automatically, including optional portable Python at runtime\python\python.exe.
- Starts the Bali dashboard through a forever engine that restarts the app if it exits.
- Creates or repairs the themed Desktop shortcut named Bali Rocket Forever.lnk.
- Opens http://127.0.0.1:9061 when the server is ready.
- Writes the latest report to shared_data\reports and the Desktop.

Safety stays unchanged:
- Public market data / research mode only.
- No API keys.
- No live orders.
- Champion lock remains locked.
